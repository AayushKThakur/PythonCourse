a = [55, 65, -95, 55.6, -95]
# a =["Aayush", "Pokemon", "Thakur"]

pos = a.index(-95)
print(pos)
# a.sort()
# a.reverse()
# a.sort(reverse=True) sort in descending order
# a.append(["Aayush", "Thakur", 100]) #appends an list at last
# a.extend(["Aayush", "Thakur", 100]) # appends each element in the existing list
r = a.count(-95)  # count of the element
# a.clear()
print(a)
